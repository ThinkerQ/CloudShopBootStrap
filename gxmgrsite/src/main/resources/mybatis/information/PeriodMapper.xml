<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PeriodMapper">
	
	<!-- 新增-->
	<insert id="save" parameterType="pd" useGeneratedKeys="true" keyProperty="period_ID">
		insert into T_period(periodNumber,	prizeUserId,needCount,productId,alreadyCount,prizeNumber,createTime,prizeTime,status)
		values (#{periodNumber},#{prizeUserId},	#{needCount},#{productId},#{alreadyCount},#{prizeNumber},#{createTime},	#{prizeTime},#{status})
	</insert>
	
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from T_period
		where 
			period_ID = #{period_ID}
	</delete>
	
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update  T_period
			set 
				periodNumber = #{periodNumber}	,	
				needCount = #{needCount}	,	
				productId = #{productId}	,	
			where
				period_ID = #{period_ID}
	</update>
	
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
			periodNumber,
			prizeUserId,
			needCount,
			productId,
			alreadyCount,
			prizeNumber,
			createTime,
			prizeTime,
			status,
			period_ID
		from 
			T_period
		where 
			period_ID = #{period_ID}
	</select>

    <sql id="selectSql">
        	a.period_ID,a.periodNumber,a.prizeUserId,a.needCount,a.alreadyCount,a.prizeNumber,a.createTime,a.prizeTime,a.status,a.productId,p.firstName
	</sql>

    <!-- 列表 -->
    <select id="datalistPage" parameterType="page" resultType="pd">
		select
        	<include refid="selectSql"/>
		from
				T_period a LEFT JOIN product p ON p.product_ID=a.productId
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
        <include refid="selectSql"/>
		from
				T_period a LEFT JOIN product p ON p.product_ID=a.productId
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from T_period
		where 
			period_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
</mapper>